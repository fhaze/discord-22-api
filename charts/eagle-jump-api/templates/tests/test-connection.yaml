apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eagle-jump-api.fullname" . }}-test-connection"
  labels:
    {{- include "eagle-jump-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eagle-jump-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
