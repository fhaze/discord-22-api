apiVersion: v1
kind: Service
metadata:
  name: {{ include "eagle-jump-api.fullname" . }}
  labels:
    {{- include "eagle-jump-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eagle-jump-api.selectorLabels" . | nindent 4 }}
